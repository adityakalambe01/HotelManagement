# =======================
# App Environment Config
# =======================

# Environment Type: production | development | test
NODE_ENV=development

# App Port
PORT=3000

# Frontend Site URL(s) allowed for CORS
# Multiple origins should be comma-separated (e.g., http://localhost:3000,https://example.com)
CORS_ORIGINS=http://localhost:3000

# =======================
# Database Config
# =======================

# MongoDB Connection URI
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority&appName=<your-app-name>

# =======================
# JWT Config
# =======================

# JWT Secret Key
JWT_SECRET=your_jwt_secret_key

# =======================
# SMTP / Email Config
# =======================

# Provider: gmail | titan | custom
SMTP_PROVIDER=gmail

# SMTP Email Address
SMTP_EMAIL=your-email@example.com

# SMTP Password / App-Specific Token
SMTP_PASSWORD=your-email-password-or-app-token

# SMTP Host (Required for titan/custom, optional for gmail)
SMTP_HOST=smtp.example.com

# SMTP Port (587 = TLS, 465 = SSL) (Required for titan/custom, optional for gmail)
SMTP_PORT=587

# SMTP Secure (true = SSL/465, false = STARTTLS/587)
SMTP_SECURE=false
